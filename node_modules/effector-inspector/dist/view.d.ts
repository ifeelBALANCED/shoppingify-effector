import { Store } from 'effector';
import { EffectMeta, EventMeta, FilesMap, LogMeta, Options, StackTrace, StoreMeta } from './types.h';
export declare function Root($stores: Store<Record<string, StoreMeta>>, $events: Store<Record<string, EventMeta>>, $effects: Store<Record<string, EffectMeta>>, $logs: Store<LogMeta[]>, $files: Store<FilesMap>, $traces: Store<StackTrace[]>, options?: Options): void;
