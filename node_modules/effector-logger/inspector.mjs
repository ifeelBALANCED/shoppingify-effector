import { createInspector as createInspector$1 } from 'effector-inspector/index.mjs';
import 'effector/effector.mjs';

const LOGGER_DOMAIN_NAME = '@effector-logger';

let initialized = false;
function createInspector() {
  if (initialized) return;

  if (typeof window !== 'undefined' && typeof window.document !== 'undefined') {
    createInspector$1({
      visible: false,
      trimDomain: LOGGER_DOMAIN_NAME
    });
    initialized = true;
  }
}
createInspector();

export { createInspector };
//# sourceMappingURL=inspector.mjs.map
